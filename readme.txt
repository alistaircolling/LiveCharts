This application can be used for live voting results using a google web form.
The app will read results from a google spreadsheet and update every 500ms.
The bar chart will animate to the new values
This has been made in a rush so the layout is not flexible and the copy hard-coded but this is easy to update.

